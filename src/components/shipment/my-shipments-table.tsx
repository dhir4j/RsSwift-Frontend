"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function MyShipmentsTable() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>My Shipments</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the user's shipments table.</p>
      </CardContent>
    </Card>
  );
}
