"use client";

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export function BookShipmentForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Book a Shipment</CardTitle>
        <CardDescription>Shipment booking form will be here.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder for the shipment booking form.</p>
      </CardContent>
    </Card>
  );
}
